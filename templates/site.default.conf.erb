# default
server {
    listen <%= port %> default_server;
    server_name _;
    location / {
        return 404;
    }
    location /server-status {
        allow 127.0.0.1;
        deny all;
        stub_status on;
    }
}
